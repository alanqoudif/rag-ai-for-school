# ادمشن - مساعد القبول الموحد

منصة ذكية مبنية على RAG (Retrieval-Augmented Generation) للإجابة على استفسارات القبول الموحد للجامعات والمؤسسات التعليمية في عُمان.

## المميزات

- 🤖 **ذكاء اصطناعي متقدم**: يستخدم GPT-4.1-nano للفهم والإجابة
- 📚 **RAG**: استرجاع المعلومات من ملف القبول الموحد
- 🎯 **دقة عالية**: الإجابات فقط من البيانات الرسمية
- ⚡ **Streaming**: ردود فورية مع البث المباشر
- 🎨 **تصميم عصري**: واجهة مستخدم جميلة وسهلة الاستخدام

## التقنيات المستخدمة

- **Frontend**: Next.js 14 + TypeScript + Tailwind CSS
- **Backend**: Next.js API Routes
- **AI**: LangChain.js + OpenAI GPT-4.1-nano
- **Vector DB**: Supabase pgvector
- **Embeddings**: text-embedding-3-small

## البدء السريع

### 1. تثبيت المتطلبات

```bash
cd admission
npm install
```

### 2. إعداد المتغيرات البيئية

أنشئ ملف `.env.local` وأضف:

```env
OPENAI_API_KEY=your_openai_api_key_here
NEXT_PUBLIC_SUPABASE_URL=https://ofpjqcijuiokevljjsju.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### 3. تحميل بيانات PDF إلى قاعدة البيانات

```bash
npm run ingest "../ملف ادمشن جديد 2.docx.pdf"
```

### 4. تشغيل المشروع

```bash
npm run dev
```

ثم افتح [http://localhost:3000](http://localhost:3000)

## بنية المشروع

```
admission/
├── app/
│   ├── api/chat/route.ts    # API للمحادثة
│   ├── page.tsx             # الصفحة الرئيسية
│   ├── layout.tsx           # التخطيط الرئيسي
│   └── globals.css          # الأنماط العامة
├── components/
│   ├── ChatInterface.tsx    # واجهة المحادثة
│   ├── Sidebar.tsx          # الشريط الجانبي
│   ├── SuggestionCards.tsx  # بطاقات الاقتراحات
│   └── CategoryTabs.tsx     # تصنيفات البرامج
├── lib/
│   ├── supabase.ts          # عميل Supabase
│   ├── langchain.ts         # إعدادات LangChain
│   └── prompts.ts           # System prompts
└── scripts/
    └── ingest-pdf.ts        # سكربت تحميل PDF
```

## كيف يعمل RAG

1. **Ingestion**: يقرأ ملف PDF ويقسمه إلى قطع صغيرة
2. **Embeddings**: يحول كل قطعة إلى vector باستخدام OpenAI
3. **Storage**: يخزن الـ vectors في Supabase pgvector
4. **Query**: عند السؤال، يبحث عن أقرب القطع المتشابهة
5. **Generation**: يمرر القطع للـ AI لتوليد إجابة دقيقة

## الملاحظات التنظيمية

الـ AI مبرمج للالتزام بالملاحظات التنظيمية:
- كل برنامج يُعامل كوحدة مستقلة
- رمز البعثة مستقل عن رمز المنحة
- الإجابات فقط من المعلومات الموجودة في الملف

## المساهمة

نرحب بمساهماتكم! افتحوا Issue أو Pull Request.

## الترخيص

MIT License
